# Python Homework

This repository contains Python programming exercises completed as part of a homework assignment. The exercises focus on Python fundamentals, input validation, unit testing with `pytest`, parameterized tests, and working with external Python packages.


# Install project dependencies with:

```bash
pip install -r requirements.txt
```

# Then run the test suite:

```bash
python -m pytest
```